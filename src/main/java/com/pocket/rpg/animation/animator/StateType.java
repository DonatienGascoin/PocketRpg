package com.pocket.rpg.animation.animator;

/**
 * Types of animator states.
 */
public enum StateType {
    /**
     * Simple state with a single animation.
     */
    SIMPLE,

    /**
     * Directional state with 4 animations (one per direction).
     */
    DIRECTIONAL
}
