package com.pocket.rpg.dialogue;

/**
 * Built-in dialogue events with engine-level meaning.
 * Handled directly by PlayerDialogueManager.
 */
public enum DialogueEvent {
    /** Ends the conversation cleanly (no further action). */
    END_CONVERSATION
}
