package com.pocket.rpg.components.dialogue;

/**
 * Predefined reactions a {@link DialogueEventListener} can perform
 * when its target event fires.
 */
public enum DialogueReaction {
    ENABLE_GAME_OBJECT,
    DISABLE_GAME_OBJECT,
    DESTROY_GAME_OBJECT,
    RUN_ANIMATION
}
